============
Installation
============

At the command line either via easy_install::

    $ easy_install pygogo

or pip::

    $ pip install pygogo

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pygogo
    $ pip install pygogo
