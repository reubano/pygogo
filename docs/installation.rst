============
Installation
============

At the command line either via easy_install::

    $ easy_install gogo

or pip::

    $ pip install gogo

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv gogo
    $ pip install gogo
